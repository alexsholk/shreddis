#!/usr/bin/env php
<?php

require __DIR__ . '/../vendor/autoload.php';

use App\Command\CopyCommand;
use Symfony\Component\Console\Application;

$app = new Application('shreddis', '1.0.0');
$app->addCommand(new CopyCommand());
$app->run();
